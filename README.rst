Implemented my own Filter which extracts the feature Surfaces
#############################################################

the tracked files have to be placed into `Filters/Core` of the vtk source path,
or find a better way to compile a custom filter.
